import {consola} from "consola";

export default defineEventHandler((event) => {
    // let riskData = null
    // consola.debug('fetching risk data ...')
    //  fetch('https://localhost:5010/api/referenceData/ForRiskRatings').then(res => res.json()).then((d) => {
    //     riskData = d.data
    //      consola.debug('risk data fetched ...')
    // }).then((riskData) => { return riskData })
    //     .catch(error => consola.error(error))
})
